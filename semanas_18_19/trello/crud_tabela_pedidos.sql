USE COMEX;

/**
CREATE TABLE [dbo].[PEDIDOS](
	[COD_PEDIDO] [bigint] NOT NULL,
	[COD_CLIENTE] [bigint] NOT NULL,
	[VALOR_TOTAL] [money] NOT NULL,
	[DESCONTO] [money] NOT NULL,
PRIMARY KEY CLUSTERED 
(
	[COD_PEDIDO] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY]
GO

ALTER TABLE [dbo].[PEDIDOS]  WITH CHECK ADD FOREIGN KEY([COD_CLIENTE])
REFERENCES [dbo].[CLIENTES] ([COD_CLIENTE])
GO
**/

INSERT INTO PEDIDOS
	VALUES
		(1000000000001, 111111, 1000.00, 100),
		(2000000000002, 222222, 10000.00, 200),
		(3000000000003, 333333, 12000.60, 500);

SELECT * FROM PEDIDOS;

SELECT VALOR_TOTAL FROM PEDIDOS;

SELECT VALOR_TOTAL FROM PEDIDOS ORDER BY VALOR_TOTAL DESC;

UPDATE PEDIDOS SET DESCONTO = 250 WHERE COD_PEDIDO = 3000000000003;

SELECT * FROM PEDIDOS;

DELETE FROM PEDIDOS WHERE COD_PEDIDO = 3000000000003;

SELECT * FROM PEDIDOS;

INSERT INTO PEDIDOS	VALUES (3000000000003, 333333, 12000.60, 500);

SELECT * FROM PEDIDOS;